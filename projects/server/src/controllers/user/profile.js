const db = require("../../models");
const user = db.User;

module.exports = {
    uploadPic: async (req,res) => {
        try {
            
        } catch (err) {
            res.status(400).send(err)
        }
    }
}